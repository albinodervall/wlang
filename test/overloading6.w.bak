#import "library/print.w"

g :: () {
}

f :: (x: i64): i64 {
    // print("Ok\n");
    // g();
    y: i64 = 2;
    return x + y;
}

f :: (x: i32): i32 {
    return f(x as i64) as i32;
}

main :: () #entry {
    f(123 as i32);
}
