#import "library/assert.w"

f :: (a: i32): i32 {
    return 1;
}

f :: (a: u64): u64 {
    return 10;
}

main :: () #entry {
    g: &(a: i32): i32 = f;
    h: &(a: u64): u64 = f;

    assert(g(2) == 1);
    assert(h(3) == 10);

    print("Ok\n");
}
