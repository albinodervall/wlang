#import "library/assert.w"

f :: (): string {
    return "Hello, world!\0\n";
}

main :: () #entry {
    s: string = f();

    print_u64(s.number_of_bytes);
    printf(s.pointer);

    printf("Ok\n\0".pointer);
}
